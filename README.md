# RunningProcessesCSVScript

This repository contains a script that creates a CSV file that lists the following information and running processes: process ID #, name, executable path, CPU usage, and mem usage. 
